{% extends 'base.html.twig' %}
{% block title %}{{ actor.name }}{% endblock %}

{% block body %}
    <div class="container-fluid p-4">
           <h1 class="mt-2">{{ actor.name }}</h1>
            <p>apparaît dans : </p>
            <ul>
                {% for program in programs %}
                    <li> <a href="/wild/show/{{ program.title |replace({' ':'-'}) |lower }}"> {{ program.title }}</a></li>
                {% endfor %}
            </ul>
        {% endblock %}

